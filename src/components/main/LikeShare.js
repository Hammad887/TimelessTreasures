import { Box, Text } from '@chakra-ui/react';
import React from 'react';

const LikeShare = ({ postId }) => {
    return (
        <Box>
            <Text>Would be the like and share functionality for post {postId}</Text>
        </Box>
    );
};

export default LikeShare;